## План: создать `D:\work\AIFlow\AGENTS.md`

### Что изменилось после вашей правки

В предыдущем плане я ошибочно пометил `.claude/commands/` слэш-команды как «Claude-Code-specific». Убираю это: **все инструменты, спроектированные в `.claude/`, считаются работающими и в этом ZCode-агенте** и будут описаны в AGENTS.md как доступные. Никаких оговорок «только для CC».

### Предполётные итоги (read-only)

- Никакого AGENTS.md нигде нет (ни в корне, ни в `.zcode/`, ни в `.agents/`, ни в дереве). Создаю первый.
- Есть подробный `CLAUDE.md` (15 КБ) и индекс `docs/`. AGENTS.md будет **кратким оперативным справочником** для ZCode-агента с прямыми ссылками в CLAUDE.md / `docs/` для деталей — без дублирования длинной аргументации.
- Замечание: `docs/16-code-map.md` и `docs/17-session-review.md` есть на диске, но отсутствуют в таблице `docs/README.md` — укажу на это в файле.

### Содержимое файла (~90–110 строк, сканируемый)

1. **Назначение и состояние монорепо** — AI Studio; Yarn 4 + Lerna; что реально (`apps/web`, `packages/db`, `packages/ui`, `tools/session-analyzer`), что объявлено-но-пусто (стабы: `apps/worker`, `services/model-router`, `services/registry-proxy`, `packages/queue`, `packages/crypto`, `packages/ai-roles`).

2. **Источник истины** — `CLAUDE.md` + `docs/`; старт с `docs/README.md` и `docs/16-code-map.md` («read, don't search»). Файлы состояния — часть коммита, не повод перепрочёсывать репо заново.

3. **Доступные инструменты (работают в этом агенте)** — отдельный раздел, без оговорок:
   - **Слэш-команды** (`.claude/commands/`): `/verify`, `/task-start <id> <slug>`, `/state-sync`, `/note <идея>`, `/session-review [window]`, `/tool-scout <need>`.
   - **Сабагенты** (`.claude/agents/`): ролевые — `analyst`, `planner`, `coder`, `reviewer`; механические read-only — `classifier`, `doc-checker`, `lang-lint`.
   - **Навыки** (`.claude/skills/`): `ai-studio-internals` (читать перед правками compose/сэндбокс/per-project БД/секретов), `notes`.
   - **MCP-серверы** (`.mcp.json`): `context7`, `omniroute`.
   - **Хуки** (`hookify.*.local.md` в `.claude/`): напр. bash-вместо-read/grep/glob, `bash-cd-prefix`, защита от деструктивных prisma-команд, `false-success-after-rm`, `webfetch-unavailable`.
   - **Тиринг моделей**: платный слот (`sonnet`/`opus` → coding) ведёт ролевых агентов; бесплатный (`haiku` → локальный Qwen в LM Studio) — `classifier`/`doc-checker`/`lang-lint`. (Алиасы условны — детали в `docs/13-agent-tooling.md` § 5.)

4. **Команды качества** — `yarn verify` (CI-гейт: typecheck → lint → format:check → test) и отдельные `typecheck`/`lint`/`format:check`/`test`; плюс скрипты пакетов (`packages/db`: `generate`, `migrate`, `seed:dev-user`, `project-sql`; `apps/web`: `dev`, `build`).

5. **Гейт реален, не рекомендателен** — `--max-warnings 0` делает `warn`-лимиты блокирующими (файл ≤200 строк, функция ≤50, complexity ≤10, depth ≤4, params ≤4); тест-файлы ≤400; `passWithNoTests: false` — ноль тестов = громкий fail.

6. **Архитектурные инварианты** — Next.js-приложение stateless и только ставит в очередь (длинное — в BullMQ-воркеры); Redis одноразовый (чекпойнт в Postgres); одна PG-схема на проект (`project_{uuid}`) против `public`; секреты AES-256-GCM под `ENCRYPTION_KEY`; сеть `sandbox` — `internal: true`.

7. **Организация кода (feature-sliced)** — в `apps/web`: `app/ → features/ → shared/ → packages/`, однонаправленно, через ESLint; `app/` — только роутинг; каждый slice — через свой `index.ts`; UI-сплит нагрузко-несущий (`packages/ui` = примитивы+токены, `apps/web/src/shared/ui` = композиция приложения); предпочитать примитивы `@aiflow/ui` и семантические токены.

8. **Гетчи** — Tailwind v4 без `tailwind.config.js` (только `@theme` в CSS); на Windows авто-детекция источников выключена (`source(none)`) → **новая директория с исходниками требует явного `@source` в `globals.css`, иначе классы исчезают молча**; `outline-hidden`, не `outline-none`; `apps/web/tsconfig.json` повторяет пути `@aiflow/*` (базовая карта заменяется, а не мержится); `packages/db` typecheck сначала гонит `generate`; `getProjectClient` держит `Map` — звать `evictProjectClient` при архивации/удалении проекта.

9. **Git-процесс** — ветка `task/{id}-{slug}` от `main`, линейная история (rebase + ff-only, `--force-with-lease`), Conventional Commits со scope workspace; рефактор на границах задач — `chore/refactor-*`, с сохранением поведения.

10. **Языковая политика** — рассуждения/тулколлы/комментарии/коммиты на английском; финальные ответы пользователю — на русском; `docs/*.md` — английский.

11. **Куда углубляться** — ссылки: `docs/15` (конвенции), `docs/14` (решения), `docs/12` (open questions + status table), `docs/13` (agent tooling), `docs/16` (code map), `docs/17` (уроки сессий), навык `ai-studio-internals` (compose/сэндбокс/секреты).

### Исполнение

Один `Write` — создание `D:\work\AIFlow\AGENTS.md`. Никаких других правок, мутаций шелла, коммитов. Содержимое — на английском (это файл-инструкция, по языковой политике проекта).
