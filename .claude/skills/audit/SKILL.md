---
name: audit
description: Shortcut for /impeccable audit. Use when the user asks to audit a UI/design surface.
---

This is a pin redirect. Load and follow the parent skill at `.cursor/skills/impeccable/SKILL.md`, then execute the `audit` command from its Commands table (reference: `reference/audit.md`).
